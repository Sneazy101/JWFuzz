Run The program with :
mvn test-compile exec:java

Put the files to be tested on src/test/java

The test file will show up on a folder with the prefix _classifytest.java in src/test/java/generated

Current data datatype that it can generate test for is int, float, boolean and String
